#include <iostream>

using namespace std;

int pivot(int arr[])
{
    int size=sizeof(arr);
    int start=0,end=size-1,mid=(start+end)/2;
    while(start>end)
    {
        if(arr[mid]>=arr[0])
            st
    }
}

int main(){
    
    return 0;
}