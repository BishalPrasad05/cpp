#include <iostream>

using namespace std;

void reverseArrayByKth(vector<int> arr[],int k)
{
    vector<int> arr2[arr.size()];
    for(int i=0;i<arr.size();i++)
    {
        if
    }
}

int main(){

    return 0;
}